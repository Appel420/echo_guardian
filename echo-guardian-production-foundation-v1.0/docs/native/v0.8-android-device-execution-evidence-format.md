# v0.8 Android Device Execution Evidence Format

The Android evidence record captures device model, Android version, app build, permission visibility, private-space block status, raw-data block status, audit checkpoint hash, signed export manifest hash, provider receipt hash, and accessibility capture hash.

The record is valid only for a physical Android device. Emulator evidence is useful for development but does not satisfy the v0.8 physical-device release gate.
