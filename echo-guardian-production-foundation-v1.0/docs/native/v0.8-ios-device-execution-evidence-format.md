# v0.8 iOS Device Execution Evidence Format

The iOS evidence record captures device model, iOS version, app build, permission visibility, private-space block status, raw-data block status, audit checkpoint hash, signed export manifest hash, provider receipt hash, and accessibility capture hash.

The record is valid only for a physical iOS device. Simulator evidence is useful for development but does not satisfy the v0.8 physical-device release gate.
