# v0.6 Native Runtime Example

This folder is reserved for native runtime hardening outputs generated by local tests and tools.

Expected artifacts include:

- `native_audit.jsonl`
- `delivery_status.jsonl`
- `native_cycle_result.json`
- `native_export/package/export_manifest.json`

No raw sensor data is retained in these artifacts.
